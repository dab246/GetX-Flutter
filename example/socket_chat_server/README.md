### Server Chat Back-End using NodeJS.

1. Install packages using below syntax -:
   > npm install

Above command will install all the necessary packages.

2. Run the app on your mobile using below command -:
   > npm run start
